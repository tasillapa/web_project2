$(function () {
    //Tooltip
    $('[data-toggle="tooltip"]').tooltip({
        container: 'body'
    });

    //Popover
    $('[data-toggle="popover"]').popover();

    //Tooltip
    $('[data-toggle-tootip="tooltip"]').tooltip({
        container: 'body'
    });

    //Popover
    $('[data-toggle-tootip="popover"]').popover();
})